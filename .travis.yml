language: java
jdk:
  - oraclejdk8
cache:
  directories:
    - $HOME/.m2
script:
  - mvn clean install
  - mvn cobertura:cobertura
after_success:
  - bash <(curl -s https://codecov.io/bash)
deploy:
  provider: script
  script:  mvn deploy -s settings.xml
  skip_cleanup: true
  on:
    tags: true